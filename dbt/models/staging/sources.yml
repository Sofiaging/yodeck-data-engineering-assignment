version: 2

sources:

  - name: raw
    schema: raw

    loaded_at_field: _ingested_at

    freshness:

      warn_after:
        count: 1
        period: day

      error_after:
        count: 2
        period: day

    tables:

      - name: accounts
      - name: subscriptions
      - name: invoices
      - name: exchange_rates
